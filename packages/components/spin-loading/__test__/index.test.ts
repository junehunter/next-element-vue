import { describe, it } from 'vitest';
import NextSpinLoading from '../index';

describe('NextSpinLoading', () => {
	it('执行 loading', async () => {
		// eslint-disable-next-line no-console
		console.log(NextSpinLoading);
	});
});
